# {Project Title}

> Generated by SwiftDocs AI

## Agent Workflow — Follow This Order

**Step 1: Install Skills**
Run these commands FIRST before doing anything else:
Skills teach your AI agent how to work on this project.

(skills commands will be listed here based on project type)

**Step 2: Scaffold the Project**
Create the project using CLI commands — never create files manually:

For MERN:
```bash
# Backend
mkdir -p backend/src/{controllers,models,routes,middleware,services,utils}
cd backend && npm init -y && npm install express@latest mongoose@latest dotenv@latest bcryptjs@latest jsonwebtoken@latest cors@latest helmet@latest
npm install -D nodemon@latest

# Frontend (always use Vite)
npm create vite@latest frontend -- --template react
cd frontend && npm install
npm install react-router-dom@latest axios@latest
```

For Flutter:
```bash
flutter create {project_name}
cd {project_name}
flutter pub upgrade
```

For Python/AI:
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install anthropic langchain openai python-dotenv fastapi uvicorn
pip freeze > requirements.txt
```

**Step 3: Read All Documentation**
After skills and scaffold are ready, read docs in this order:
1. PRD.md — product vision and requirements
2. TechStack.md — what to use and why
3. DatabaseSchema.md — data models
4. FolderStructure.md — where everything goes
5. AgentSystemPrompts.md — your full instructions
6. ClaudeContext.md — project context for AI

**Step 4: Build**
Now start implementing, following the documentation.
Always verify package versions are latest before installing.
